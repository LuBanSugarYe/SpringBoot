# DataSource Config
spring:
  datasource:
    driver-class-name: org.h2.Driver
    schema: classpath:data/schema-h2.sql
    data: classpath:data/data-h2.sql
    url: jdbc:h2:mem:test
    username: root
    password: test

# Logger Config
logging:
  level:
    com.sugar.mybatis_plus_demo: debug
